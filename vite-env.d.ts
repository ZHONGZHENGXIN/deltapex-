// Fix: Use the standard Vite client type reference to avoid duplicate identifier errors for asset modules.
// This directive includes the necessary types for assets (SVG, PNG, JPG, etc.) provided by Vite.
/// <reference types="vite/client" />
